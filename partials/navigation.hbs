<ul class="navbar-nav mr-auto links">
    {{#foreach navigation}}
    <li class="nav-{{slug}} nav-item {{#if current}}active{{/if}}"><a href="{{url absolute="true"}}" class="nav-link">{{label}}</a></li>
    {{/foreach}}
</ul>

<style>
    .nav-item {
        font-family: WorkSans-ExtraBold !important;
        font-weight: 900;
    }
</style>