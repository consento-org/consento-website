<ul class="navbar-nav mr-auto links">
    {{#foreach navigation}}
    <li class="nav-{{slug}} nav-item {{#if current}}active{{/if}}"><a href="{{url absolute="true"}}">{{label}}</a></li>
    {{/foreach}}
</ul>

<style>
    .nav-item a {
        font-family: WorkSans-Bold;
        font-weight: 900;
    }

     .nav-item a:hover {
        text-decoration: none;
    }
</style>